APP DIRECTORY
++++++++++++++


This is the directory to add the application layer files and it's views.

This frontend folder contains every module and its views related to the frontend of the system or website.

The backend folder contains every thing related to the application backend where administrators or supervisors will use the application to process and configure the application.